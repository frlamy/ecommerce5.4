<li class="nav-item dropdown">
    <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">{{ main_title }}</a>
    <div class="dropdown-menu" style="">

        <{{ header }} class="dropdown-header">{{ header_title }}</{{ header }}>

        {% for entity in entities %}
            <a class="dropdown-item" href="{{ entity.url }}">{{ entity.name }}</a>
        {% endfor %}

        <div class="dropdown-divider"></div>

        <a class="dropdown-item text-bold" href="{{ footer_url }}">Voir tout</a>
    </div>
</li>
